[Settings]
# Added automatically by the Netlify CLI. It has no effect during normal
# Git-backed deploys.
ID = "37e42309-db08-474a-b7ad-c16ebbf6a8cc"

# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]
  # Directory to change to before starting a build.
  # This is where we will look for package.json/.nvmrc/etc.
  base = "docs"

  # Directory (relative to root of your repo) that contains the deploy-ready
  # HTML files and assets generated by the build.
  publish = "build"

  # Default build command.
  command = "npm run build"

  # Directory with the lambda functions to deploy to AWS.
  # functions = "project/functions/"

[build.environment]
  NODE_VERSION = "18.19"

# Production context: all deploys from the Production branch set in your site's
# deploy settings will inherit these settings.
# [context.production]
  # publish = "output/"
  # command = "make publish"
  # environment = { ACCESS_TOKEN = "super secret", NODE_ENV = "8.0.1" }

# Deploy Preview context: all deploys resulting from a pull/merge request will
# inherit these settings.
# [context.deploy-preview]
  # publish = "dist/"

[[redirects]]
from = "/web-api-template"
to = "/features/web-api-template"

[[redirects]]
from = "/servicebus-queue-worker-template"
to = "/features/servicebus-queue-worker-template"

[[redirects]]
from = "/servicebus-topic-worker-template"
to = "/features/servicebus-topic-worker-template"

[[redirects]]
from = "/azurefunctions-http-template"
to = "/features/azurefunctions-http-template"

[[redirects]]
from = "/azurefunctions-databricks-jobmetrics-template"
to = "/features/azurefunctions-databricks-jobmetrics-template"
